"""Account-age lookup for /scam risk assessment.

Честный провайдер: дата создания аккаунта **недоступна** ни через Bot API,
ни через MTProto — Telegram убрал поле ``User.date`` / ``UserFull.registration_date``
из текущего слоя протокола (проверено на live-сессиях, 2026-08). Поэтому
функция всегда возвращает ``None``, и строка «Аккаунт создан больше 5 месяцев
назад» не выводится, чем мы избегаем ложной информации.

Если в будущем появится источник возраста аккаунта (внешний сервис, своя БД,
плагин), реализуй его здесь и верни возраст в днях — ветка в хендлере
(scam.py, ``scam_risk_age``) уже готова и сработает без изменений.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)

# Порог «больше 5 месяцев» (в днях) — константа для ветки риска.
ACCOUNT_AGE_RISK_DAYS = 150


async def get_account_age_days(user_id: int) -> int | None:
    """Возвращает возраст аккаунта в днях или None (источник недоступен).

    ``user_id`` оставлен в сигнатуре, чтобы подключение реального источника
    не потребовало изменения вызова.
    """
    return None
